{
  "name": "brick-email-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for forwarding emails to Brick API",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "dependencies": {
    "@sentry/cloudflare": "^8.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.26.1"
  }
}
